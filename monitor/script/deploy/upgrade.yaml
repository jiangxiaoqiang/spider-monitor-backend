- hosts: spider-monitor-app-server
  remote_user: root
  tasks:
  - name: start agent
    shell: "/bin/bash /home/dolphin/app/backend/spider-monitor-backend/upgrade-app.sh >> a.log"
    async: 100
    poll: 20
    register: copy_configuer_log
    tags:
      - start_agent
  - name: show execute result
    debug: var=copy_configuer_log
